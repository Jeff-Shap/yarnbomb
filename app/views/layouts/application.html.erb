<!DOCTYPE html>
<html>
<head>
  <title>YarnBomb2</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<h1> YarnBomb </h1>

<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <ul class="nav navbar-nav navbar-left">
      <li> <button type="button" class="btn btn-default navbar-btn">YARNBOMB</button></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <% if notice %>
      <p class="alert alert-success navbar-text"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger navbar-text"><%= alert %></p>
    <% end %>
      <% if user_signed_in? %>
        <p class="navbar-text">Logged in as <strong><%= current_user.email %></strong></p>
      <% end %>
      <li>
          <button type="button" class="btn btn-default navbar-btn">About</button>
        </li>
      <% if user_signed_in? %>
      <li><button type="button" class="btn btn-default navbar-btn">Projects</button></li>
      <li><button type="button" class="btn btn-default navbar-btn">Yarns</button></li>
        <li>
          <ul class="nav nav-pills">
            <li role="presentation" class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                <button type="button" class="btn btn-default navbar-btn">Profile <span class="caret"></span></button>
              </a>
              <ul class="dropdown-menu">
                <li>
                  <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %>
                </li>
                <li>
                  <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
                </li>
              </ul>
            </li>
          </ul>       
        </li>
      <% else %>
        <li>
          <button type="button" class="btn btn-default navbar-btn">
            <a href="/users/sign_up">Register</a>
          </button>
        </li>
        <li>
          <button type="button" class="btn btn-default navbar-btn">
            <a href="/users/sign_in">Sign in</a>
          </button>
        </li>
        
      <% end %>
    </ul>
  </div>
</nav>




<%= yield %>

</body>
</html>
